# Runtime dependencies for the deployed Streamlit app — kept minimal for fast, reliable
# cloud builds. Tooling used only to build/refresh the crane data lives in requirements-ingest.txt.
streamlit>=1.30
matplotlib>=3.7
pandas>=2.0
